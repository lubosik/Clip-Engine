# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
*.so

# Alembic
alembic.ini.local

# Storage — raw downloads and rendered clips are on the Railway volume, not in git
storage/
/data/

# Assets — campaign media (logo, outro, font) IS committed: the Docker image
# bakes assets/ in at build time, so Railway needs them in the repo.
# Wizard-created campaign assets live on the volume (see deploy/start.sh).

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# pytest
.pytest_cache/
htmlcov/
.coverage

# Logs
*.log
logs/
