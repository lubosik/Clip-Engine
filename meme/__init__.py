"""
meme/ — reference-based meme engine for Clip Engine.

Public surface:
    meme.profile      — extract_profile, get_active_profile
    meme.generate     — generate_memes
    meme.classifier   — classify, violates_hard_rules
    meme.text_posts   — generate_text_posts
    meme.feedback     — promote_top_performers
    meme.run          — CLI entrypoint (python -m meme.run)
"""
